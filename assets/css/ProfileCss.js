import { StyleSheet } from "react-native";

const profileCss = StyleSheet.create({
    title: {
        marginTop: 20,
        color: '#888686',
        fontWeight: 'bold',
    },
    container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    data: {
        color: '#1E1D1D'
    }
});

export {profileCss};