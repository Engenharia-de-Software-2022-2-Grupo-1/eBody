import { StyleSheet } from "react-native";

const profileCss = StyleSheet.create({
    title: {
        marginTop: 10,
        color: '#888686',
        fontWeight: 'bold',
    },
    container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    data: {
        color: '#1E1D1D'
    },
    espacamento: {
        marginTop: 10

    }
});

export {profileCss};